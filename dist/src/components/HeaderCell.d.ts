import { Props as WrappedProps } from './HeaderCellWrapped';
interface Props {
    columnIndex: number;
    isDragDisabled: boolean;
}
export declare function HeaderCell(props: Props & WrappedProps): JSX.Element;
export {};
